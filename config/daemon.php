<?php
/**
 * Config Definition Format: 
 * 
 * 'tasks' => array(
 *		'<eventname>' => array(
 *          'callback' => <callback definition>,
 *			'interval' => <interval in seconds>,
 *      ),
 *	),
 *
 * e.g.
 *	'tasks' => array(
 *		 'test' => array(
 *			'callback' 	=> array( 'Controller_Test', 'daemon' ),
 *			'interval' 	=> 60,
 *		),
 *	)
 *
 */
return array(
	'tasks' => array(
	)
);